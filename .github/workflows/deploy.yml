name: Deploy from dev branch

on:
  push:
    branches: ["dev"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: "dev"

      - name: Setup Pages
        uses: actions/configure-pages@v3

      - name: Copy files from frontend/docs
        run: |
          echo "Копируем файлы из frontend/docs/"
          mkdir -p _site  # Создаем папку _site
          cp -r frontend/docs/* _site/
          cp -r frontend/docs/.* _site/ 2>/dev/null || true
          echo "Содержимое папки _site:"
          ls -la _site/

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site # Явно указываем путь

  deploy:
    environment:
      name: github-pages-dev
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v3
